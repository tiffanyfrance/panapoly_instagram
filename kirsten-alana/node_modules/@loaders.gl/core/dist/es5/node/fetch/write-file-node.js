"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.writeFile = writeFile;
exports.writeFileSync = writeFileSync;

var _fs = _interopRequireDefault(require("fs"));

var _util = require("util");

var _assert = _interopRequireDefault(require("../../utils/assert"));

function writeFile(filePath, arrayBufferOrString) {
  return (0, _util.promisify)(_fs["default"].writeFile)("".concat(filePath), toBuffer(arrayBufferOrString), {
    flag: 'w'
  });
}

function writeFileSync(filePath, arrayBufferOrString) {
  return _fs["default"].writeFileSync("".concat(filePath), toBuffer(arrayBufferOrString), {
    flag: 'w'
  });
}

function toBuffer(binaryData) {
  if (ArrayBuffer.isView(binaryData)) {
    binaryData = binaryData.buffer;
  }

  if (typeof Buffer !== 'undefined' && binaryData instanceof ArrayBuffer) {
    var buffer = new Buffer(binaryData.byteLength);
    var view = new Uint8Array(binaryData);

    for (var i = 0; i < buffer.length; ++i) {
      buffer[i] = view[i];
    }

    return buffer;
  }

  return (0, _assert["default"])(false);
}
//# sourceMappingURL=write-file-node.js.map